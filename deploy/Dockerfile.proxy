# Reverse proxy con la config horneada (no bind-mount → funciona por contexto remoto).
FROM nginx:1.27-alpine
COPY ghost-proxy.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
HEALTHCHECK --interval=30s --timeout=3s --retries=3 \
  CMD wget -q -O /dev/null http://127.0.0.1:80/ || exit 1
